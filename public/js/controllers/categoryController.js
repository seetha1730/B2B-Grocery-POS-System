//const tbody = document.querySelector('#category table tbody')

window.addEventListener("load", (event) => {
//   APIGetCall("/category/all")
//   .then(response => response.json())
//   .then(data => {
//     const categories = data.categoryList;
//     console.log(categories)// Replace with the actual data property name
//     renderCategoryTable(categories);
//   })
//   .catch(error => {
//     console.error("Error fetching category data:", error);
//   });
// });


// function renderCategoryTable(categories) {
//   const tbody = document.querySelector('#category tbody');

//   // Clear any existing rows in the table body
//   tbody.innerHTML = '';

//   // Loop through the categories and create rows
//   categories.forEach((category, index) => {
//     const row = document.createElement('tr');
//     row.innerHTML = `
//       <td>${index}</td>
//       <td class="category-image"><img src="${category.imageUrl}" alt="${category.categoryName}" /></td>
//       <td>${category.categoryName}</td>
//       <td>${category.parentCategory}</td>
//       <td>
//         <button class="btn btn-primary">Edit</button>
//         <button class="btn btn-danger">Delete</button>
//       </td>
//     `;

//     tbody.appendChild(row);
 });


// Call the function to render the table with sample data

